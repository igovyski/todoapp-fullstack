<main>
    <section class="tasks">

        {{#each tasks}}
        <div class="task">
            <form action="/complete" method="post">
                <div>
                    <input type="checkbox" id="task-{{this.id}}" onchange="completeTask({{this.id}})">
                    <label for="task-{{this.id}}">{{this.description}}</label>
                </div>

                <button>
                    <img src="/images/x-icon.png" alt="">
                </button>
            </form>
        </div>
        {{/each}}

        <div class="info">
            <p>{{quantityActiveTasks}} active tasks</p>

            <div class="buttons">
                <a href="/">All</a>
                <a href="/actives" class="active">Actives</a>
                <a href="/completed">Completed</a>
            </div>

            <a href="">Clean tasks</a>
        </div>
    </section>

    <section class="buttons">
        <a href="" class="active">All</a>
        <a href="">Actives</a>
        <a href="">Completed</a>
    </section>
</main>